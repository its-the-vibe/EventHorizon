server:
  host: ""       # leave empty to listen on all interfaces
  port: 8080

redis:
  host: "localhost"
  port: 6379
  db: 0
  channel: "eventhorizon"

log_level: "info"  # DEBUG, INFO, WARN, ERROR (case-insensitive; also settable via LOG_LEVEL env var)
